Assign Role
<%= form_for [@role, @user], url: user_role_path(@user, @role) do |f|  %>
<%= f.collection_select :user_id, User.all, :id, :title %>
<%= f.hidden_field :assigner_id, value: current_user%>
<%= f.submit "Assign Role"%>
<% end %>