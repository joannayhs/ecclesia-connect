<%= form_for :user_role do |f|  %>
<%= f.collection_select :user_id, User.all, :id, :name%>
<%= f.date_select :date_assigned, order: [:month, :day, :year]%>
<%= f.hidden_field :assigner_id, value: current_user%>
<%= f.submit "Assign Role"%>
<% end %>