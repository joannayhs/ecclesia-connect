<%= @role.title %><br> 
<%= @role.description%><br>
<%= link_to @role.team.name, team_path(@role.team) %><br>
 
<%= assign_role %>


<%# Button for editing role 
Button for deleting role  %>

<%# admin can assign roles to users %>
<%# updates user_roles table with user_id, role_id, assigned_by(current_user.id), date %>
