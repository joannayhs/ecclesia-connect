
<h1>Welcome, <%= @user.name %></h1>

<h2>Upcoming Roles:</h2> 
<table>
<tr>
<th>Date</th>
<th>Role</th> 
<th>Arrival Time</th>
</tr> 

<tr>
    <% @user.roles.each do |role| %>
    <td><%= role.date %></td>
    <td><%= link_to role.title, role_path(role.id) %></td>
    <td><%= role.arrival_time %></td>
    <%end%>

<tr> 
</table> 

<%# would like to be able to sort this table by role, team, date, assigned by %>
<%# users can change the status of their roles to confirm and can change the date in the user_roles %>
